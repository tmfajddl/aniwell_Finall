<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.RSW.repository.PetRecommendationRepository">

    <insert id="insert" parameterType="PetRecommendation">
        INSERT INTO pet_recommendation
            (memberId, type, name, address, phone, mapUrl)
        VALUES
            (#{memberId}, #{type}, #{name}, #{address}, #{phone}, #{mapUrl});
    </insert>

    <select id="findByMemberAndName" resultType="PetRecommendation">
        SELECT * FROM pet_recommendation
        WHERE memberId = #{memberId} AND name = #{name}
    </select>

    <select id="findByMember" resultType="PetRecommendation">
        SELECT * FROM pet_recommendation
        WHERE memberId = #{memberId}
        ORDER BY createdAt DESC
    </select>

    <delete id="deleteById">
        DELETE FROM pet_recommendation
        WHERE id = #{id}
    </delete>

</mapper>
