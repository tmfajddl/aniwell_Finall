<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.RSW.repository.PetRepository">

	<!-- 펫 목록 조회 -->
	<select id="getPetsByMemberId" parameterType="int"
		resultType="com.example.RSW.vo.Pet">
		SELECT *
		FROM pet p
		WHERE p.memberId = #{memberId}
	</select>

	<!-- 펫 삭제 -->
	<delete id="deletePet" parameterType="int">
		DELETE FROM pet WHERE id =
		#{id}
	</delete>

	<!-- 펫 등록 -->
	<insert id="insertPet">
		INSERT INTO pet
		(memberId, name, species, breed,
		gender, birthDate, weight, photo)
		VALUES
		(#{memberId}, #{name},
		#{species}, #{breed}, #{gender}, #{birthDate}, #{weight},
		#{photo})
	</insert>

	<!-- 마지막 삽입된 ID 조회 -->
	<select id="getLastInsertId" resultType="int">
		SELECT LAST_INSERT_ID();
	</select>

	<!-- 펫 수정 (사진 없이) -->
	<update id="updatePetWithoutPhoto" parameterType="map">
		UPDATE pet
		SET
		name = #{name}, species = #{species}, breed = #{breed},
		gender =
		#{gender}, birthDate = #{birthDate}, weight = #{weight}
		WHERE id =
		#{petId}
	</update>

	<!-- 펫 수정 (사진 포함) -->
	<update id="updatePet" parameterType="map">
		UPDATE pet
		SET name =
		#{name}, species = #{species}, breed = #{breed},
		gender = #{gender},
		birthDate = #{birthDate}, weight = #{weight}, photo =
		#{photo}
		WHERE id
		= #{petId}
	</update>

	<!-- 펫 ID로 조회 -->
	<select id="getPetsById" parameterType="int"
		resultType="com.example.RSW.vo.Pet">
		SELECT *
		FROM pet p
		WHERE p.id = #{petId}
	</select>

	<select id="findPetsWithBirthdayInDays"
		resultType="com.example.RSW.vo.Pet">
		SELECT *
		FROM pet
		WHERE DATE_FORMAT(birthDate, '%m-%d') IN (
		DATE_FORMAT(DATE_ADD(NOW(), INTERVAL 0 DAY), '%m-%d'),
		DATE_FORMAT(DATE_ADD(NOW(), INTERVAL 3 DAY), '%m-%d'),
		DATE_FORMAT(DATE_ADD(NOW(), INTERVAL 7 DAY), '%m-%d')
		)
	</select>

	<!-- ✅ 가장 최근 체중 1건 조회 -->
	<select id="findLastWeightByPetId" parameterType="int"
		resultType="double">
		SELECT weightKg
		FROM pet_weight_log
		WHERE petId = #{petId}
		ORDER BY measuredAt DESC
		LIMIT 1
	</select>

	<!-- ✅ 체중 로그 INSERT -->
	<insert id="insertWeightLog">
		INSERT INTO pet_weight_log
		SET petId = #{petId},
		measuredAt = NOW(),
		weightKg = #{weightKg},
		source = #{source},
		note =
		#{note}
	</insert>

	<!-- ✅ 펫 현재 체중 업데이트 ※ pet 테이블의 실제 컬럼명이 'weight'임을 기존 매퍼에서 확인했습니다. ※ 별도의 
		updatedAt/weightUpdatedAt 컬럼이 없으면 아래 두 줄만 두세요. -->
	<update id="updatePetWeight">
		UPDATE pet
		SET weight = #{weightKg}
		WHERE id = #{petId}
	</update>

	<!-- ✅ 최근 등록된 펫 ID 조회 (동명이인 대비: memberId + name) -->
	<select id="findNewestPetIdByMemberAndName" resultType="int">
		SELECT id
		FROM pet
		WHERE memberId = #{memberId}
		AND name = #{name}
		ORDER BY id DESC
		LIMIT 1
	</select>

	<!-- ✅ 최신 급여량 조회 -->
	<!-- - pet_feed_log 테이블에서 해당 펫(petId)의 최근 급여 기록을 가져옴 - amountG: 급여량(g) - 
		fedAt 기준으로 내림차순 정렬 후 1건만 조회 (최신 데이터) -->
	<select id="findLastFeedAmountByPetId" parameterType="int"
		resultType="double">
		SELECT amountG
		FROM pet_feed_log
		WHERE petId = #{petId}
		ORDER
		BY fedAt DESC
		LIMIT 1
	</select>


	<!-- ✅ 급여 로그 INSERT -->
	<!-- - 새로운 급여 기록을 적재 - fedAt: 자동으로 NOW() 입력 (급여 시각) - amountG: 급여량(g) - 
		foodName: 제품명 - feedType: 건식/습식/간식 등 타입 - brand: 브랜드명 - source: 기록 출처(manual, 
		device 등) - note: 메모 (예: 아프거나 먹은 양 변동 사유) -->
	<insert id="insertFeedLog">
		INSERT INTO pet_feed_log
		SET petId = #{petId},
		fedAt =
		NOW(),
		amountG = #{amountG},
		foodName = #{foodName},
		feedType =
		#{feedType},
		brand = #{brand},
		source = #{source},
		note = #{note}
	</insert>
</mapper>
